# DiskClean Agent

绿色单 exe 磁盘清理工具：服务方远程协助客户时，客户下载工具、输入服务方签发的密钥（默认 1 天有效期），自助完成任意磁盘的扫描与清理。纯本地运行、无广告、无上传、删除即卸载。

## 最新版：v0.2.5（2026-08-17）

### 本次更新

- **时钟容差收紧为 4 小时**（原 24h，审查发现恶意客户可利用首次拨慢时钟把 1 天密钥用成约 2 天）；"未生效"与"时间回拨"拆分为两种提示文案
- **回拨防护加 1 小时容差**：客户机时钟先快后被 Windows 校时回调，不再被误判为"回拨作弊"
- 时间异常提示更准确："密钥尚未生效"与"检测到系统时间被回拨"分开显示

### 版本历史

| 版本 | 日期 | 内容 |
|---|---|---|
| v0.2.5 | 2026-08-17 | 时钟容差 24h→4h；lastSeen 回拨容差 1h；时间异常文案拆分（独立代码审查后修复） |
| v0.2.4 | 2026-08-16 | 密钥验证时钟容差（修复先无效后有效）；密钥设备绑定 v3（keygen 设备码 + 客户界面设备码展示/复制） |
| v0.2.3 | 2026-08-16 | 回收站清理修复；失败项显示原始错误 |
| v0.2.2 | 2026-08-16 | 密钥格式 v2：有效期改为签发时刻起精确 N×24 小时（旧 v1 密钥全部作废）；keygen 增加管理密码门禁、历史密钥记录（有效/过期分区，可一键重发）、模板未注入守卫、主密钥指纹显示；exe 验证成功显示精确到期时刻 |
| v0.2.1 | 2026-08-16 | 切换磁盘后扫描/清理按所选盘过滤（非系统盘仅回收站·本盘）；空间分布 TOP10 超时 5→10 分钟；进度条改确定式 0→100 |
| v0.2 | 2026-08-16 | 扫描进度条+动效、已选/共统计、管理员接口（X-Admin-Key 三端点 + 服务方管理面板） |

## 使用方法

| 文件 | 给谁 | 用法 |
|---|---|---|
| `dist/DiskCleanAgent.exe` | 客户 | 双击 → UAC 提权 → 浏览器自动打开 → 选盘扫描（免费）→ 输密钥 → 勾选清理（界面显示本机设备码，绑定密钥需先发给服务方） |
| `dist/keygen.html` | 服务方 | 双击打开 → 输管理密码 → 填客户备注 + 有效期（+ 可选客户设备码）→ 生成密钥（含历史记录） |

## 特性

- 零运行时依赖单 exe（Node 24 SEA 打包），离线 HMAC 密钥验证（客户机无需联网）
- 密钥有效期（签发时刻起精确 N×24 小时，1/2/3/7/30 天可选，4h 时钟容差）+ 时间回拨防护（1h 校时回调容差）+ 可选设备绑定（设备码烙入密钥，其他设备提示已绑定）
- 扫描免费、清理需密钥；失败诊断（安全软件检测 + 建议 + 单项目重试）
- 非系统盘白名单底线：仅可清该盘回收站（防误删客户数据）

## 已知限制

- exe 未做代码签名：客户机 SmartScreen 会提示"未知发布者"，需点"更多信息 → 仍要运行"
- keygen 历史记录存于生成密钥的那台电脑的浏览器（localStorage），换电脑/清浏览器数据会丢

## 开发

- 测试：`npm test`（79/79）；构建：`npm run build`（产物在 dist/，gitignored）
- 项目权威记录见 `PROJECT.md`（阶段、硬性约定、已知限制、里程碑）
